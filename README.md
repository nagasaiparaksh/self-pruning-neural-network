# self-pruning-neural-network
Built a self-pruning neural network using PyTorch with learnable gates that remove unimportant weights during training. Used L1 regularization for sparsity and trained on CIFAR-10 to study the trade-off between accuracy and model compression.
